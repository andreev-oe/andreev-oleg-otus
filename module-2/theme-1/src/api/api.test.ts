import { noop } from './noop';

describe('Тест API курсов', () => {
  test('Получение списка курсов', () => {
    expect(noop()).toBe(true);
  })
  test('Создание курса', () => {
    expect(noop()).toBe(true);
  })
  test('Редактирование курса', () => {
    expect(noop()).toBe(true);
  })
  test('Удаление курса', () => {
    expect(noop()).toBe(true);
  })
  test('Выставление оценки курсу', () => {
    expect(noop()).toBe(true);
  })
})

describe('Тест API занятий', () => {
  test('Получение списка занятий курса', () => {
    expect(noop()).toBe(true);
  })
  test('Создание занятия курса', () => {
    expect(noop()).toBe(true);
  })
  test('Редактирование занятия курса', () => {
    expect(noop()).toBe(true);
  })
  test('Удаление занятия курса', () => {
    expect(noop()).toBe(true);
  })
  test('Выставление оценки занятию', () => {
    expect(noop()).toBe(true);
  })
})

describe('Тест API комментариев', () => {
  test('Получение списка комментариев', () => {
    expect(noop()).toBe(true);
  })
  test('Создание комментария', () => {
    expect(noop()).toBe(true);
  })
  test('Редактирование комментария', () => {
    expect(noop()).toBe(true);
  })
  test('Удаление комментария', () => {
    expect(noop()).toBe(true);
  })
})

describe('Поиск курса по сайту', () => {
  test('Получение списка курсов по поиску', () => {
    expect(noop()).toBe(true);
  })
})
